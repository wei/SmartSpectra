version: '3.8'
services:
  backend:
    build: ./web/server
    ports:
      - "8080:8080"
  frontend:
    build: ./web/example
    ports:
      - "3000:3000"
    environment:
      - VITE_SERVER_URL=http://backend:8080
    depends_on:
      - backend
